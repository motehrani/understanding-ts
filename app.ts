const userName = "Max";

console.log(userName);
// instead of tsc app.ts every time we make some changes
// tsc app.ts -w
// or
// tsc app.ts --watch

// but the question is what if we have lot of files...
// in that position we got to do tsc command for each fucking file??
// so than we should say tsc --init in the terminal and than say just tsc
